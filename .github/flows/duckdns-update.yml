name: Update DuckDNS

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update DuckDNS
        run: |
          curl -s "https://www.duckdns.org/update?domains=goestest966.duckdns.org&token=${{ secrets.DUCKDNS_TOKEN }}&ip="
